# Database
DATABASE_URL="postgresql://vkf:vkf_dev_password@localhost:5432/vaibhav_kashi_foods?schema=public"

# Auth — generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-a-long-random-secret"
# Optional alias used by some hosts
NEXTAUTH_SECRET="replace-with-a-long-random-secret"

# Public site URL (no trailing slash)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Seeded admin account (change immediately after first login)
ADMIN_EMAIL="admin@vaibhavkashifoods.local"
ADMIN_PASSWORD="ChangeMeNow!VKF"

# Razorpay (leave blank to keep online payments disabled until configured)
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
NEXT_PUBLIC_RAZORPAY_KEY_ID=""

# Maps (optional — address-based directions still work without this)
NEXT_PUBLIC_GOOGLE_MAPS_KEY=""

# WhatsApp ordering (digits only, with country code, e.g. 918437360273)
NEXT_PUBLIC_WHATSAPP_NUMBER="918437360273"

# Optional: cron / internal API protection
INTERNAL_API_SECRET=""
